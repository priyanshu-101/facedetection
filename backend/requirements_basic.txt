# Core dependencies (required)
Flask==2.3.3
Flask-CORS==4.0.0
pymongo==4.5.0
opencv-python==4.8.1.78
numpy==1.24.3
Pillow==10.0.1
python-dotenv==1.0.0
werkzeug==2.3.7

# Note: bson is included with pymongo, no need to install separately

# Optional: Advanced face recognition (requires Visual C++ build tools on Windows)
# Uncomment these lines if you have Visual Studio build tools installed:
# dlib==19.24.1
# face-recognition==1.3.0

# Alternative: Use opencv-contrib-python for additional OpenCV features
# opencv-contrib-python==4.8.1.78
